import { OnInit, ElementRef, OnChanges } from '@angular/core';
import { FormGroup, FormControl } from '@angular/forms';
import { MatMenuService } from '../shared/services';
export declare class DlvMatWaybillComponent implements OnInit, OnChanges {
    private menuService;
    placeHolder: string;
    isAutoFocus: boolean;
    waybillForm: FormGroup;
    waybillNo: FormControl;
    waybillInputRef: ElementRef;
    private waybills;
    constructor(menuService: MatMenuService);
    private isFocusEvent;
    ngOnInit(): void;
    ngOnChanges(): void;
    /**
     * @name createFormControls
     * @description This method is assigning value to input field
     */
    private activateFocus;
    private createFormControls;
    /**
     * @name createForm
     * @description It is containing waybill no
     */
    private createForm;
    /**
     * @name searchWaybill
     * @description this method is using to get the searched waybill detail on seperate window.
     */
    searchWaybill(): void;
    ngOnDestroy(): void;
}
