import { OnInit, EventEmitter } from '@angular/core';
import { CookiesService, MatCenterService, MatMenuService } from '../shared/services';
export declare class DlvMatCenterModalComponent implements OnInit {
    private centerService;
    private cookies;
    private menuService;
    open: boolean;
    isAllCenter: boolean;
    onCenterChange: EventEmitter<any>;
    private env;
    private center;
    private showCloseBtn;
    private centerDetails;
    constructor(centerService: MatCenterService, cookies: CookiesService, menuService: MatMenuService);
    ngOnInit(): void;
    /**
     * @name closeSearchCenter
     * @description This method is using to close center search modal after select center from modal
     */
    closeSearchCenter(): void;
    /**
     * @name onCenterSelect
     * @description this function works after select center from search box in modal page, store result in cookie
     * @param center : It is storing center code and center name
     */
    onCenterSelect(center: any): void;
}
