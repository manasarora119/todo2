import { OnInit, EventEmitter } from '@angular/core';
import { CookiesService, MatCenterService, MatMenuService } from '../shared/services';
export declare class DlvMatCenterComponent implements OnInit {
    private centerService;
    private menuService;
    private cookies;
    open: boolean;
    isAllCenter: boolean;
    onCenterChange: EventEmitter<any>;
    closeDropdown: EventEmitter<any>;
    private centerDetails;
    private centerArray;
    private env;
    constructor(centerService: MatCenterService, menuService: MatMenuService, cookies: CookiesService);
    ngOnInit(): void;
    ngDoCheck(): void;
    /**
     * @name selectCenter
     * @description this function is used to select center from search dropdown and storing it into cookies as well as show the status 'new', close dropdown and emit the selected center
     * @param center : this is containing select center code and center name
     */
    selectCenter(center: any): void;
}
