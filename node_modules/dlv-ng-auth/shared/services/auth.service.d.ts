import { AuthDetailsService } from '../services/auth-details.service';
import { UserService } from '../services/user.service';
export declare class AuthService {
    private authDetailsService;
    private userService;
    constructor(authDetailsService: AuthDetailsService, userService: UserService);
    init(customOptions: any): void;
    isAuthenticated(): boolean;
    getUserProfile(data: any): any;
    changePassword(data: any): import("rxjs/internal/Observable").Observable<{}>;
    logout(): void;
}
