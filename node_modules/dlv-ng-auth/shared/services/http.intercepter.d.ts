import { HttpInterceptor, HttpEvent, HttpHandler, HttpRequest } from '@angular/common/http';
import { Observable } from 'rxjs';
import 'rxjs/add/operator/do';
import { AuthDetailsService } from './auth-details.service';
export declare class DlvHttpInterceptor implements HttpInterceptor {
    private authDetailsService;
    constructor(authDetailsService: AuthDetailsService);
    intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>>;
    /**
     * To update body
     * @param request : HttpRequest
     */
    private updateBody;
    /**
     * To andle request error
     * @param error
     */
    private catchErrors;
    /**
     * To set request header
     * @param req : HttpRequest
     */
    private setReqHeader;
    private beforeRequest;
}
