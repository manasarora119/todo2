import { OnInit, EventEmitter } from '@angular/core';
import { EncryptService, AuthService, AuthDetailsService } from '../shared/services';
export declare class InitLoginComponent implements OnInit {
    private authDetailsService;
    private encrypt;
    private authService;
    private optionConstant;
    private loginButtonDetails;
    private appHeading;
    private loginMessage;
    showError: boolean;
    private redirectUrl;
    private configOptions;
    private config;
    private callbackUrl;
    callAfterLogin: EventEmitter<any>;
    callOnUnauthorized: EventEmitter<any>;
    constructor(authDetailsService: AuthDetailsService, encrypt: EncryptService, authService: AuthService);
    ngOnInit(): void;
    private openLogin;
}
